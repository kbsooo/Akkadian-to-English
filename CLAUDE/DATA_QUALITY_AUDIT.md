# Data Quality Audit Report
## GPT 전처리 결과 품질 검증

---

## Executive Summary

| 항목 | 값 | 평가 |
|------|-----|------|
| 전체 문장 쌍 | 3,265 | - |
| 유효 쌍 (Valid) | 2,998 (91.8%) | ✅ 양호 |
| 평균 Quality Score | 0.856 | ✅ 양호 |
| 고품질 (0.85+) | 1,959 (60.0%) | ✅ 양호 |

**종합 평가: 데이터 품질 양호, 일부 주의 필요 영역 존재**

---

## 1. Quality Score 분포

```
Very Low (0-0.3):     0개 (0.0%)
Low (0.3-0.5):       16개 (0.5%)   ⚠️ 재검토 필요
Medium (0.5-0.7):   405개 (12.4%)  ⚠️ 선별적 사용
High (0.7-0.85):    885개 (27.1%)  ✅ 사용 가능
Very High (0.85+): 1,959개 (60.0%) ✅ 우선 사용
```

---

## 2. 주요 문제점 분석

### 2.1 정렬 불일치 (Alignment Mismatch) ⚠️ 주의

**발견:** ~1,028개 쌍에서 잠재적 정렬 문제 감지

**문제 유형:**
1. **"qí-bi-ma" 구문 불일치**
   - SRC: `qí-bi4-ma šu-ma a-ha-tí a-ta...` (말하기 서두)
   - TGT: `Air the -textiles that I left...` (완전히 다른 내용)
   - → 문장 경계 탐지 실패로 추정

2. **"um-ma X-ma" 불일치**
   - SRC: `um-ma a-tí-na-ma É-tum...` (From X: 구문)
   - TGT: 중간 문장부터 시작
   - → Rule-based segmentation 오류

**영향:** 모델이 잘못된 번역 패턴 학습 위험

**권장 조치:**
```python
# Strict alignment check 추가
def strict_alignment_check(src, tgt):
    # "um-ma X-ma" → "From X" or "Thus X says"
    if re.match(r'um-ma\s+[\w-]+-ma', src):
        if not re.match(r'(From|Thus|Say)', tgt, re.I):
            return False, 'umma_mismatch'

    # "a-na X qí-bi-ma" → "To X:"
    if 'qí-bi' in src[:50]:
        if not re.match(r'To\s+\w+', tgt, re.I):
            return False, 'qibi_mismatch'

    return True, None
```

### 2.2 숫자 불일치 (Number Mismatch) ⚠️ 경미

**발견:** 1,282개 쌍 (39.3%)

**분석 결과:**
- 대부분 **양성 케이스** (False Positive)
- 원인:
  1. 숫자가 영어 단어로 번역됨 (5 → "five")
  2. 아카드어 분수 표기 (0.33333 → "one-third")
  3. 단위 변환 (GÍN → "shekel" 개념 통합)

**예시:**
```
SRC nums: {'1', '14', '1.5'}
TGT nums: {} (빈 집합)
→ 숫자가 "14 shekels", "one and a half" 등으로 번역됨
```

**권장 조치:**
- number_mismatch를 치명적 오류에서 제외 (현재 설정 유지)
- 학습에는 문제 없음

### 2.3 과도한 Unknown 마커 ⚠️ 주의

**발견:** 88개 쌍에서 `<unk_sign>` 비율 > 20%

**영향:**
- 모델이 `<unk_sign>` → 다양한 영어 출력 연결 학습
- 추론 시 불안정한 출력 가능

**권장 조치:**
```python
# unk_ratio > 0.3인 경우 학습에서 제외 권장
valid_df = df[df['unk_ratio'] <= 0.3]  # 현재 설정과 동일
```

### 2.4 짧은 문장 쌍 ⚠️ 경미

**발견:** 617개 쌍에서 SRC < 5 토큰

**분석:**
- 대부분 문서 시작부 인사말/서두
- 예: `a-na IŠTAR-lá-ma-sí` → `To Nitahšušar:`

**평가:**
- 짧지만 올바른 번역 쌍
- 모델 학습에 유용 (간단한 패턴 학습)
- **제외 불필요**

---

## 3. 소스별 품질 비교

| 소스 | 개수 | 평균 품질 | Valid Rate |
|------|------|----------|------------|
| Rule-based | 2,889 | **0.867** | 96.4% |
| Annotated | 376 | 0.776 | 63.3% |

**발견:**
- **역설적으로 Rule-based가 더 고품질**
- Annotated 소스의 낮은 Valid Rate (63.3%)

**원인 분석:**
- Annotated는 `Sentences_Oare_FirstWord_LinNum.csv` 기반
- First word 매칭으로 segment 추출 → 경계가 부정확
- Rule-based는 명확한 마커 (`um-ma`, `IGI` 등) 사용

**권장 조치:**
- Annotated 소스 중 `quality_score >= 0.7`만 사용
- 또는 Annotated를 Rule-based 결과로 검증

---

## 4. 최종 권장 데이터셋 구성

### Option A: Conservative (보수적)
```python
# 고품질만 사용
final_df = df[
    (df['is_valid'] == True) &
    (df['quality_score'] >= 0.7) &
    (df['unk_ratio'] <= 0.25)
]
# 예상: ~2,400개 쌍
```

### Option B: Balanced (균형)
```python
# 현재 valid 기준 유지 + 정렬 체크 추가
final_df = df[
    (df['is_valid'] == True) &
    (df['quality_pass'] == True) &
    (~alignment_mismatch)  # 추가 필터
]
# 예상: ~2,700개 쌍
```

### Option C: Aggressive (공격적)
```python
# 모든 valid 쌍 사용 (현재 상태)
final_df = df[df['is_valid'] & df['quality_pass']]
# 현재: 2,998개 쌍
```

---

## 5. 조치 사항 체크리스트

### 필수 ✅
- [x] Quality score 분포 확인
- [x] 낮은 품질 샘플 수동 검토
- [x] Alignment 문제 식별

### 권장 📋
- [ ] `qí-bi-ma` / `um-ma` 정렬 검증 로직 추가
- [ ] Annotated 소스 중 저품질 필터링
- [ ] 학습 전 최종 샘플 50개 추가 검토

### 선택 🔧
- [ ] Back-translation으로 정렬 검증
- [ ] Rule-based 경계 마커 확장
- [ ] Cross-validation으로 노이즈 내성 테스트

---

## 6. 결론

**현재 데이터셋 품질: B+ (양호)**

| 장점 | 단점 |
|------|------|
| 충분한 양 (3,000개) | ~31% 정렬 불확실 |
| 고품질 비율 높음 (60%+) | Annotated 소스 품질 낮음 |
| 다양한 문서 유형 포함 | 숫자 변환 검증 어려움 |

**학습 진행 권장:**
- 현재 `sentence_pairs_valid.csv` 사용 가능
- 정렬 의심 케이스 (~1,000개)는 모니터링 필요
- 학습 후 validation loss 추이로 노이즈 영향 평가

---

*Generated: 2026-01-29*
*Auditor: Claude*
